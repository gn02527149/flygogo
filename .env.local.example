# Supabase — 複製為 .env.local 並填入你的專案值（Dashboard → Settings → API Keys）。
# 未設定時 app 會使用內建的 in-memory 示範資料（重啟即重置、不用登入）。
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# 伺服器端管理金鑰（Secret keys）— 註冊帳號用，絕不可外流或加 NEXT_PUBLIC_ 前綴。
SUPABASE_SERVICE_ROLE_KEY=

# Google Flights 報價（透過 SerpApi 的 google_flights engine）。
# 未設定時使用內建模擬報價，整個掃描/警示流程仍可運作。
SERPAPI_KEY=
